title: PreAssessment MIC
description: PreAssessment and Interview
template:
  repo: preassessment-mic-template
  name: preassessment-mic
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
